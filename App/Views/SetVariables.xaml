<Window x:Class="FolderCreator.Views.SetVariables"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Ustaw Zmienne" Height="300" Width="400">
    <Grid>
        <StackPanel Margin="10">
            <ItemsControl ItemsSource="{Binding Variables}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <TextBlock Text="{Binding Key}" Width="100" VerticalAlignment="Center" />
                            <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Width="200" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10">
                <Button Content="OK" IsDefault="True" Width="75" Margin="0,0,10,0" Click="OkButton_Click" />
                <Button Content="Anuluj" IsCancel="True" Width="75" Click="CancelButton_Click" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
